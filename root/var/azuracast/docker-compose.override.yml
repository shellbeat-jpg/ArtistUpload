cat > /var/azuracast/docker-compose.override.yml <<'YAML'
services:
  mariadb:
    command:
      - mysqld
      - --innodb-buffer-pool-size=256M
      - --innodb-log-buffer-size=16M
      - --max-connections=40
      - --table-open-cache=256
YAML


      
 

